# 安全與隱私說明

**T-SCHOOL Schedule Sync** 會為你自動讀取臺北市數位實驗高級中等學校（以下簡稱本校）的課程規劃表與課綱，同步行程至個人 Google 日曆，並發送相關通知。本程式在你自己的 Google 帳號與 Google 文件副本（行程同步控制臺）中執行；除了請求課表時會送出年級參數，以及連接 Google 服務以讀取課綱、管理專用行事曆與寄送通知信外，本程式不會主動將你的個人資訊傳送至其他外部服務。專案作者亦不會因你對本程式授權而取得對應權限。

為了完成自動化同步，Google 會在第一次開啟控制臺時要求授予本程式以下權限。Google 授權畫面的實際文字與分組可能不同，但本專案的 `appsscript.json` 明確列出以下 8 項權限。


### 1. 查看目前 Google 帳號的電子郵件地址 `userinfo.email`

- **用途**：比對目前登入的 Google 帳號與設定的通知 Email，防止誤用其他帳號同步。
- **程式內安全限制**：僅會讀取目前帳號的 Email 地址，無法取得帳號密碼，也無法讀取或搜尋 Gmail 收件匣。

### 2. 查看及管理已安裝這個應用程式的文件 `documents.currentonly`

- **用途**：在此份 Google 文件中顯示控制臺與相關操作介面。
- **程式內安全限制**：就這項文件權限而言，無法存取你個人雲端硬碟中的其他 Google 文件（Docs）；課綱試算表則依下方的唯讀權限讀取。

### 3. 查看您的所有 Google 試算表檔案 `spreadsheets.readonly`

- **用途**：讀取校內課綱資料。
- **程式內安全限制**：僅會讀取課綱索引與其中列出的課綱試算表；無法修改、寫入或刪除任何試算表。

> 由於 Google 未提供「唯讀且僅限程式指定檔案」的授權範圍選項，且課綱需隨學期更新，故無法限定僅授權讀取特定檔案。

### 4. 查看、編輯、共用及永久刪除您可透過 Google 日曆存取的所有日曆 `calendar`

- **用途**：在你指定且由你擁有的日曆中建立及更新同步事件；可以選擇主要日曆、其他自有日曆，或另建專用日曆。
- **程式內安全限制**：每份控制臺會建立一個綁定目前 Apps Script 專案的隨機隱藏事件權杖。日期範圍搜尋只有在同步識別碼、管理標記與控制臺權杖同時相符時才會辨識事件；沒有完整標記的事件即使標題、時間、地點與說明完全相同也不會被接管。更新與刪除只從本工具保存的 Event ID 開始並再次驗證標記；受管理事件疑似被手動刪除時，系統先補回並要求從控制臺確認刪除範圍，Email 本身不執行日曆寫入。危險的全日曆刪除功能固定停用。選取既有日曆不會改變其名稱、說明或顏色。

### 5. 連線至外部服務 `script.external_request`

- **用途**：連線至本校課程規劃表 API 端點擷取行程、讀取專案 GitHub 倉庫上固定版本的通知信範本。
- **程式內安全限制**：僅會讀取上述兩項外部資源；請求課表時僅送出年級參數，不會附加你的通知 Email、Calendar ID、行程選擇或同步狀態。

### 6. 以您的身份傳送電子郵件 `script.send_mail`

- **用途**：當行程出現異動、程式完成同步或發生錯誤時，寄送通知信至你填寫的電子郵件地址。
- **程式內安全限制**：正確操作下，程式只會發送信件至你設定的通知電子郵件地址，若此設定缺失，則會改寄至目前執行程式的帳號。程式無法讀取或搜尋你的 Gmail 收件匣。

### 7. 允許這個應用程式在未受您監控的狀態下自動執行 `script.scriptapp`

- **用途**：關閉這份 Google 文件後，程式仍能在背景自動檢查並同步最新行程。
- **程式內安全限制**：僅執行指定的行程同步、課綱更新與通知排程，可隨時在控制臺側欄中關閉自動同步。

### 8. 在 Google 應用程式內的提示和側欄中顯示及刊登第三方網頁內容 `script.container.ui`

- **用途**：用於在 Google 文件內部呈現設定對話框與控制臺側欄介面。
- **程式內安全限制**：僅於目前文件內渲染控制介面。

若仍有疑慮，可在「擴充功能」→「Apps Script」查看完整程式碼，並請可信任且具程式經驗的人或 AI 協助檢查；無法確認時，則先不要授權。

請勿將這份 Google 文件控制臺副本的編輯權限分享給其他人，因為文件編輯者可能修改其中的 Apps Script。尋求協助時，也請勿提供完整設定碼、通知 Email 或 Calendar ID。

停止使用時，請先關閉自動同步；若不想保留行程，再使用控制臺的「移除受管理事件」。若使用的是本工具另建的專用日曆，也可以自行刪除整個日曆；若使用主要或其他既有日曆，請勿刪除整個日曆。之後可前往 [Google 帳戶的第三方連結](https://myaccount.google.com/connections)，選擇本工具→「查看詳細資料」→「移除存取權」→「確認」。撤銷授權不會自動刪除已同步到日曆上的行程。

完整操作方式可參考 [Google 官方的第三方連結管理教學](https://support.google.com/accounts/answer/13533235?hl=zh-Hant)。

---

謝齊宣　2026 年 8 月 11 日
