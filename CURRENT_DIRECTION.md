# CURRENT_DIRECTION

> 本文件記錄目前階段的產品、UI、互動與設計方向，內容可能隨專案演進而更新。
> 修改相關區域前應閱讀本文件，但不得將歷史方向視為永久架構契約。
> 最近整理：2026-07-22。

## 目前產品重點

- 維持第一次使用者能理解並完成設定的清楚流程。
- 優先提升美感、創新性、T-SCHOOL 品牌辨識與動態方向，再進行嚴格的視覺系統清理。
- 視覺探索不得破壞既有資料載入、驗證、程式碼產生、同步安全與公開 Apps Script 介面。
- UI 文案維持繁體中文、簡潔、以行動為導向，且不假設使用者具備程式背景。

## UI 基準與封存資料

- 在沒有使用者最新要求或更新設計規格時，以 `configurator/` 中目前已實作的介面與元件模式作為後續修改基準。
- 歷史規劃資料位於 `archive/`，除非使用者明確要求，不得作為目前設計依據。
- 先前嚴格的 Material Design 3 規則已封存於 `archive/visual-design-material3/`，目前不啟用。
- `1Campus/` 不得作為視覺或互動參考。

## 目前介面結構

- 安裝器採單一直向敘事流程。
- 設定步驟以自然高度的平面區段由上而下排列，不使用 sticky 卡片堆疊或卡片內嵌捲動。
- 非目前區段以 progressive blur 呈現深度；目前以五層不同半徑的 `backdrop-filter` 搭配漸層 mask 交錯近似連續變化，霧層橫向覆蓋完整區段以避免卡片側邊裁切斷層；尚未解鎖的下一區段只顯示模糊預覽，其後區段隱藏。
- 漸進式模糊的整體行程與強度集中由 `--fog-layer-bleed` 與 `--fog-blur-1` 至 `--fog-blur-5` 控制；各層 `--fog-mask` 的漸層百分比用於微調每一階模糊交錯的起訖位置。
- 緊鄰當前卡片的上一區段使用獨立五層 progressive blur 降低前一卡片與完成按鈕的注意力殘留；霧層錨定於上一區段底部並以固定上限避免長卡片攤薄漸層，五階模糊由遠離當前卡片的一端向連接邊界累積遞減。承載五層 `backdrop-filter` 的容器不得使用 `mask-image`、`filter` 或 `opacity` 建立 Backdrop Root，以免只能模糊霧色而無法取樣後方文字；邊緣淡出改由透明背景漸層與各子層 mask 處理。覆蓋行程由 `--past-fog-height` 控制，各半徑清除位置由 `--past-fog-layer-1-end` 至 `--past-fog-layer-5-end` 控制，邊界與強度另由 `--past-fog-entry-end`、`--past-fog-boundary-clear-start`、`--past-fog-blur-1` 至 `--past-fog-blur-5`、`--past-fog-opacity` 控制。
- Hero 採左右「文字／紙張轉換」構圖；桌機紙張固定、行程卡隨捲動向右移，手機則由捲動帶動紙張整組向左且行程卡在畫面座標向右移，並以跟隨紙張的五層水平 progressive blur 呈現課表資料逐漸清晰的轉換。Hero 霧層必須隔離於主視覺堆疊中，圖層高於紙張與行程卡、低於文案與 CTA，且將左側與垂直濾鏡邊界延伸至可視範圍之外，避免矩形邊界、閃爍與其他區域色彩滲入；桌機與手機分別使用 `--hero-fog-desktop-*`、`--hero-fog-mobile-*` 參數，手機紙張場景不得侵入底部 CTA 的保留區。
- 第二至第四步必須按下位於兩張卡片之間的區段完成按鈕，才會解鎖下一步；未解鎖區段不得顯示其完成按鈕輪廓。
- 第一步不預選年級，也不顯示第一、二張卡片間的完成按鈕；選擇年級並成功讀取課表後，直接以動畫前往第二步。
- 課表來源狀態未選年級時保持白色背景；讀取中使用可重用的淺橘色背景 token，左側狀態條與底部進度動畫使用既有橘色色票；讀取成功恢復綠色填色。
- 第一步選擇年級後只由被選按鈕呈現選取回饋，不得在整組三個年級按鈕外圍播放共用綠色光暈或底色。
- 第一至第四步依序採「年級與課表狀態」、「課程／活動搜尋與選擇」、「Email／通知時間」、「可返回修改的設定摘要」架構；第二、四步的課程與活動名稱依繁體中文筆畫排序，第四步以 Hero 同語彙的線框標籤呈現各項名稱；未出現在目前介面的舊篩選、同步預設與進階設定控制不再顯示。
- 第二步搜尋欄有內容時，右側控制改為等視覺重量的取消 icon；點擊或在搜尋欄按 `Escape` 均清除搜尋並恢復完整清單，輸入法組字期間不得誤觸。
- 步驟標題、步驟位置、選擇按鈕、圖示按鈕與設定摘要列優先以共用元件模式維護，並統一卡片內按鈕的 hover、active 與 reduced-motion 狀態。
- 選擇型按鈕的桌面 hover 僅使用中性描邊與低對比陰影，與綠色填色及綠色位移陰影的已選狀態明確區隔；無 hover 或粗略指標裝置取消 hover 視覺，只保留瞬時 `:active` 按壓回饋與 `:focus-visible` 鍵盤焦點。
- 程式碼輸出區段與設定步驟分離。
- 卡片間的連接線直接接觸上下卡片；當前卡片下方的完成按鈕保持清楚並位於遮罩上方，當前卡片上方已使用過的按鈕則納入 progressive blur，只有接至當前卡片的下段線條逐漸從霧中變清楚。
- 使用 contextual cursor motion 輔助呈現流程；游標本體以逐像素灰階與反相在背景邊界同時呈現深色／淺色，外圍只保留一圈半徑約 11 px、較細、較淺且隨移動速度輕微伸縮的光圈，不產生鮮豔互補色，且動畫不得阻礙任務完成。
- 觸控操作與 reduced-motion fallback 必須保持可用。
- Google Sheet 設定側欄延續安裝器的四色調色盤、緊湊圓角、4 px 間距網格、線條式區段層級與響應式動態語言；不得恢復封存的 Material 3 卡片樣式。

## 設計系統維護

- `configurator/styles.css` 最後的共用區塊定義 4 px 間距尺度與字級／行高 token。
- 在可行時，先調整共用 token，再新增元件專用的孤立數值。
- 主觀美感、層級、節奏、對齊、對比、響應式構圖與品牌一致性由使用者進行最終判斷。

## 動態與捲動

- 主要動態參數集中於 `configurator/app.js` 的 `MOTION_CONFIG`；優先修改集中設定，避免在事件處理器中加入零散常數。
- 平滑頁面捲動由同檔案頂部的 `ENABLE_SMOOTH_SCROLL` 開關；設為 `false` 即可停用 Lenis 與程式觸發的平滑捲動，reduced-motion 偏好仍優先停用。
- `MOTION_CONFIG` 控制區段解鎖轉場、桌面與觸控捲動速率、邊界減速曲線、active section activation line 與首頁進入第一步的捲動時間。
- 頁面頂部、底部與未解鎖區段邊界共用單向漸近式減速：在抵達前縮小手勢位移量並停在邊界，不產生超出位移或回彈。
- 邊界手感優先調整 `boundarySlowdownDistanceDesktop` / `boundarySlowdownDistanceMobile`、`boundarySlowdownExponent`與 `boundaryMinimumFactor`；桌面與觸控的整體反應另由 `scrollLerpDesktop` / `scrollLerpTouch` 及對應 multiplier 控制。
- 自訂游標只使用深色與淺色兩種狀態，依游標下方背景明度切換；保留輕微跟隨延遲，但不得使用互補色反相。
- 「開始設定」、「複製程式碼」與同層級主要動作使用低彩度、低對比的共用 hover 陰影，不以互補色陰影建立層級。
- 首頁進入第一步與完成動作後進入下一步，都使用一般頁面捲動動畫；reduced-motion 模式改為立即定位。
- 區段切換後可將標題設為程式化焦點以供輔助技術理解位置，但 `tabindex="-1"` 的非互動標題不得顯示瀏覽器文字選取式外框。
- 使用者可在已解鎖區段間捲動或由階段選單返回，但不得由捲動或階段選單越過尚未完成的區段。
- 尚未完成目前區段時，向下捲動的終點須讓當前卡片中心到達或越過畫面中央，並露出下一張模糊卡片的一小段；接近終點時須非線性減速並停在邊界，不得回彈或讓焦點脫離目前區段。
- 第二步完成按鈕第一次觸發時播放點擊動畫、切換為「再檢查一遍確認沒問題 ↵」後恢復未選取狀態，第二次觸發才切換為選取狀態並前往第三步；第三、四步的完成按鈕第一次觸發即選取並前往下一步。
- 當焦點不在輸入、選單、連結或其他互動元件，且未處於輸入法組字狀態時，Enter 可觸發目前區段的完成按鈕；實際變更選課或通知設定會重設相關確認狀態並重新鎖定下游步驟。

## 控制臺範本

- 安裝步驟提供 `configurator/assets/t-school-control-panel-template.xlsx` 作為 Google Sheets 可匯入的預設控制臺；範本只說明安裝與日常操作，不要求使用者在儲存格中填寫設定。
- 公開 Google Sheets 範本準備完成後，將 `configurator/app.js` 的 `GOOGLE_SHEETS_TEMPLATE_COPY_URL` 設為該試算表的 `/copy` 網址，介面即改為「建立自己的控制臺副本」連結；未設定時維持下載本機範本的誠實 fallback。

## 更新方式

當目前方向改變時：

1. 直接更新本文件，使其描述「現在採用的方向」，不要累積長篇時間線。
2. 已停用但仍可能需要追溯的規格移至 `archive/`。
3. 若改變涉及長期產品契約、資料模型或安全機制，另同步更新 `PROJECT_CONTEXT_FOR_AI.md`。
4. 若改變形成每次任務都必須遵守的穩定規則，再將精簡版本提升至 `AGENTS.md`。
