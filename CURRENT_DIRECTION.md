# CURRENT_DIRECTION

> 本文件記錄目前階段的產品、UI、互動與設計方向，內容可能隨專案演進而更新。
> 修改相關區域前應閱讀本文件，但不得將歷史方向視為永久架構契約。
> 最近整理：2026-07-24。

## 目前產品重點

- 維持第一次使用者能理解並完成設定的清楚流程。
- 優先提升美感、創新性、T-SCHOOL 品牌辨識與動態方向，再進行嚴格的視覺系統清理。
- 視覺探索不得破壞既有資料載入、驗證、程式碼產生、同步安全與公開 Apps Script 介面。
- UI 文案維持繁體中文、簡潔、以行動為導向，且不假設使用者具備程式背景。

## UI 基準與封存資料

- 在沒有使用者最新要求或更新設計規格時，以根目錄公開設定器目前已實作的介面與元件模式作為後續修改基準。
- 歷史規劃資料位於 `archive/`，除非使用者明確要求，不得作為目前設計依據。
- 先前嚴格的 Material Design 3 規則已封存於 `archive/visual-design-material3/`，目前不啟用。
- `1Campus/` 不得作為視覺或互動參考。

## 目前介面結構

- 安裝器採單一直向敘事流程。
- 設定步驟以自然高度的平面區段由上而下排列，不使用 sticky 卡片堆疊或卡片內嵌捲動。
- 非目前區段以 blur 呈現深度；尚未解鎖的唯一下一區段預覽使用五層不同半徑的 `backdrop-filter` 搭配漸層 mask 交錯近似連續變化，霧層與各取樣層橫向超出完整區段以避免卡片或程式碼框側邊產生裁切斷層；已解鎖但非目前區段的卡片統一改用單一全區段霧層。
- 漸進式模糊的整體行程與強度集中由 `--fog-layer-bleed` 與 `--fog-blur-1` 至 `--fog-blur-5` 控制；各層 `--fog-mask` 的漸層百分比用於微調每一階模糊交錯的起訖位置。
- 已解鎖但非目前聚焦的區段（包含卡片下方的完成按鈕）使用單一全區段 `backdrop-filter`；切換焦點時只在 `0px` 與 `--unlocked-section-blur` 之間動畫模糊半徑。這套單層霧化同時用於目前卡片上方與下方的已解鎖區段，避免往回捲動後下方卡片重新啟用五層濾鏡。
- 捲動切換焦點以卡片實際內容區間與畫面中央線的距離判定；中央線仍位於目前卡片內時不切換，離開後另以向上較寬、向下較窄的遲滯距離避免邊界抖動。霧層容器固定存在，離開前後各半個 viewport 觀察範圍的圖層以 `visibility` 停止繪製；只有尚未解鎖的下一張預覽保留五層固定半徑、opacity 交叉淡入淡出的漸進霧化。
- Hero 採左右「文字／紙張轉換」構圖；桌機與手機都由捲動帶動課表、日曆紙張整組由右向左移動，桌機行程較短，並與最後一張課程／活動卡片同時抵達終點。桌機保留完整功能標籤文案，手機採「標題／紙張動畫／精簡功能標籤／CTA」排列並省略介紹句。手機三張課程／活動卡片的起點與終點分別依課表、日曆紙張的實際位置、表頭高度與列高計算，置於兩張紙張的對應表格列中央，並沿小幅上拱曲線由左往右移動；Hero 高度跟隨 `visualViewport`，CTA 使用正常排版與 safe-area 間距，避免 Safari 底部工具列裁切。紙張、連接線與行程卡位於不同 Z 深度，桌機以右近左遠的固定透視為基準並隨精細游標小幅轉動，陰影隨視差反向偏移；觸控與 reduced-motion 只保留較弱的靜態透視，不要求裝置方向感測權限。五層水平 progressive blur 跟隨紙張的捲動位移，但維持在 3D 場景之外的畫面平面，不隨游標轉動；圖層高於紙張與行程卡、低於文案與 CTA，且將左側與垂直濾鏡邊界延伸至可視範圍之外，避免矩形邊界、閃爍與其他區域色彩滲入。Hero 超出畫面前後 `640px` 後會取消待執行的捲動／3D 動畫幀並暫停模糊圖層繪製，重新接近時才重算版面。手機版因 `.hero-copy` 使用 `display: contents`，標題與功能標籤必須各自建立高於霧層的堆疊層級，不得只依賴已不存在的父層盒子；桌機與手機分別使用 `--hero-fog-desktop-*`、`--hero-fog-mobile-*` 參數。
- 第二至第四步必須按下位於兩張卡片之間的區段完成按鈕，才會解鎖下一步；未解鎖區段不得顯示其完成按鈕輪廓。
- 第一步不預選年級，也不顯示第一、二張卡片間的完成按鈕；選擇年級並成功讀取課表後，直接以動畫前往第二步。
- 課表來源狀態未選年級時保持白色背景；讀取中使用可重用的淺橘色背景 token，左側狀態條與底部進度動畫使用既有橘色色票；讀取成功恢復綠色填色。
- 第一步選擇年級後只由被選按鈕呈現選取回饋，不得在整組三個年級按鈕外圍播放共用綠色光暈或底色。
- 第一至第四步依序採「年級與課表狀態」、「課程／活動搜尋與選擇」、「Email／通知時間」、「可返回修改的設定摘要」架構；通知時間可新增至最多四個不重複時段，最後一個時段同時作為每日成功摘要時間。第二步在該年級課表含寒暑假行程時分為「學期間課程」、「學期間活動」、「寒暑假期間課程 / 活動」，否則維持「課程」、「活動」。第二、四步的課程與活動名稱依繁體中文筆畫排序，第四步以 Hero 同語彙的線框標籤呈現所有設定值，未填警告使用系統橘色；未出現在目前介面的舊篩選、同步預設與進階設定控制不再顯示。
- 第二步搜尋欄有內容時，右側控制改為等視覺重量的取消 icon；點擊或在搜尋欄按 `Escape` 均清除搜尋並恢復完整清單，輸入法組字期間不得誤觸。
- 步驟標題、步驟位置、選擇按鈕、圖示按鈕與設定摘要列優先以共用元件模式維護，並統一卡片內按鈕的 hover、active 與 reduced-motion 狀態。
- 選擇型按鈕的桌面 hover 僅使用中性描邊與低對比陰影，與綠色填色及綠色位移陰影的已選狀態明確區隔；無 hover 或粗略指標裝置取消 hover 視覺，只保留瞬時 `:active` 按壓回饋與 `:focus-visible` 鍵盤焦點。
- 程式碼輸出區段與設定步驟分離。
- 第五步採深色「變出控制臺」卡片：`Code.gs` 不隨設定即時重建，只在第四步按下「產生安裝程式碼」時產生或更新；完成後先保留一個短暫的瀏覽器繪製間隔，再開始第四至第五步的切換。第五張預覽與進場動畫沿用共用模糊半徑，但以 `--preview-fog-start-opacity`、`--preview-fog-end-opacity`、`--preview-fog-layer-opacity` 與 `--section-reveal-start-opacity` 覆寫透明度，避免淺色霧層讓深色卡片瞬間變亮。程式碼預覽下半部使用純黑透明度漸層降低閱讀壓力，不套用模糊濾鏡；預覽不建立內嵌捲動，也不得以 `data-lenis-prevent` 排除頁面的 Lenis 捲動，文字仍可選取。主要複製按鈕以疊層置中覆蓋於預覽上。尚未複製時的桌面 hover 只浮起並產生低對比陰影，不改變填色，游標按下時立即下沉，放開才執行複製。複製成功後切換為無外框的綠色「再次複製 ↵」狀態且維持可再次按下；第五步聚焦時也可直接按 Enter 複製，並播放下沉再回彈的按壓回饋。後續指引固定為建立控制臺試算表、新增擴充功能、完成第一次同步設定三列，第一列直接呈現使用者的通知 Email；第二列附 Google 試算表「擴充功能 → Apps Script」示意圖，只有「模板連結」與兩處「程式碼」文字作為可操作連結。
- 卡片間的連接線直接接觸上下卡片；當前卡片下方的完成按鈕保持清楚並位於遮罩上方，當前卡片上方已使用過的按鈕則納入 progressive blur，只有接至當前卡片的下段線條逐漸從霧中變清楚。
- 使用 contextual cursor motion 輔助呈現流程；游標本體以逐像素灰階與反相在背景邊界同時呈現深色／淺色，外圍只保留一圈半徑約 11 px、較細、較淺且隨移動速度輕微伸縮的光圈，不產生鮮豔互補色，且動畫不得阻礙任務完成。複製程式碼成功時，不顯示右下角通知，改由目前複製媒介的游標補充標籤以綠底白字顯示「複製完成！」。
- 觸控操作與 reduced-motion fallback 必須保持可用。
- 行動裝置聚焦輸入欄位時，以 `visualViewport` 的實際可視高度與位移計算鍵盤遮擋；只暫時放寬目前步驟的捲動邊界至足以顯示聚焦欄位，不得解鎖或切換至下一步。鍵盤收起、裝置旋轉或分割畫面改變後重新計算版面並恢復一般卡片邊界；卡片維持自然頁面捲動，不增加卡片內嵌捲動。
- 第三步 Email 驗證失敗時使用卡片內提示、`aria-invalid` 與自動聚焦，不呼叫瀏覽器原生 constraint-validation 浮窗；唯一可見預覽的五個 `backdrop-filter` 圖層保持為穩定合成層，避免 Email 狀態重繪時被 Chromium 暫時撤除再重建而產生單幀閃爍。
- 行動裝置使用 Lenis `syncTouch` 的逐幀 `lerp` 平滑，Safari 鍵盤與候選字列動畫期間先合併連續的 `visualViewport` 事件，待可視區域穩定後再校正一次聚焦欄位位置，避免立即捲動與 viewport 更新互相回授。
- Google Sheet 設定側欄延續安裝器的四色調色盤、緊湊圓角、4 px 間距網格、線條式區段層級與響應式動態語言；不得恢復封存的 Material 3 卡片樣式。主要設定區段依序使用「同步狀態」、「日曆」、「年級」、「課程與活動」、「通知」等簡短標題，欄位與操作則沿用「收通知的 Email」、「通知時間」等網站語彙；若課表含寒暑假行程，課程選項同樣分成「學期間課程」、「學期間活動」、「寒暑假期間課程 / 活動」。通知時間可新增至四個，並直接作為自動同步時段，最後一個時段兼作每日成功摘要。建立新專用日曆時可自行命名，預設格式為「{年級}行程｜T-SCHOOL Schedule Sync」。第一次同步會先以獨立程序準備未來 30 天課綱，連同首批行程通常約 1–3 分鐘；全部背景批次在行程很多時可能需 10–20 分鐘。執行期間以後端階段與已處理事件數回報真實進度。
- 通知 Email 只保留一套標準版型，採白底「行事曆通知單」：綠色表示完成、橘紅表示行程調整或需處理。外框使用 1 px 狀態色，標籤為直角不可點按樣式，按鈕與內容區塊沿用網站 4 px 圓角；不得使用單側厚色框。錯誤信先說明現況與既有資料是否保留；所有語句移除句號，底部只顯示「這封信由你的行程同步控制臺自動寄出」。

## 設計系統維護

- 根目錄 `styles.css` 最後的共用區塊定義 4 px 間距尺度與字級／行高 token。
- 在可行時，先調整共用 token，再新增元件專用的孤立數值。
- 主觀美感、層級、節奏、對齊、對比、響應式構圖與品牌一致性由使用者進行最終判斷。

## 動態與捲動

- 主要動態參數集中於根目錄 `app.js` 的 `MOTION_CONFIG`；優先修改集中設定，避免在事件處理器中加入零散常數。
- 平滑頁面捲動由同檔案頂部的 `ENABLE_SMOOTH_SCROLL` 開關；設為 `false` 即可停用 Lenis 與程式觸發的平滑捲動，reduced-motion 偏好仍優先停用。
- `MOTION_CONFIG` 控制區段解鎖轉場、手動捲動的 `lerp`、鎖定邊界的回穩時間、鍵盤 viewport 穩定等待時間、active section activation line 與首頁進入第一步的捲動時間。
- 手動滾輪、觸控板與觸控捲動使用 Lenis 的逐幀 `lerp`；不得在 Lenis 建構設定加入全域 `duration`，避免瀏覽器漏幀後依經過時間突然追趕。完成按鈕、階段選單、回到頁首及鎖定步驟邊界仍可各自指定 `duration`。
- Lenis 使用內建 `autoRaf` 驅動手動與程式捲動，不另以事件喚醒、`isScrolling` 判定休眠的自訂 RAF 迴圈，避免卡片狀態更新或文件邊界讓動畫在臨界幀提前停止。
- 所有步驟解鎖後，頁面只使用 Lenis 的文件邊界與連續動量，不另設頁尾攔截，也不因單次極小反向輸入立即重設捲動目標。尚未解鎖下一步時則只將最終目標封頂於目前流程邊界，不縮小每次手勢的輸入距離。
- 手動捲動手感優先調整 `scrollLerp`、`scrollTouchLerp`、`scrollWheelMultiplier` 與 `scrollTouchMultiplier`；鎖定步驟的一般邊界收斂調整 `boundarySettleLerp`。從 Hero 快速滑向第一個鎖定邊界時，若 Lenis 目標位置明顯領先畫面，另以 `initialBoundaryMinLerp` 與 `initialBoundaryBlendDistanceRatio` 混合成較柔和的收斂速度；程式捲動則調整 `sectionTransitionDuration`、`homeEntryScrollDuration` 與 `footerReturnScrollDuration`。
- 自訂游標只使用深色與淺色兩種狀態，依游標下方背景明度切換；保留輕微跟隨延遲，但不得使用互補色反相。
- 「開始設定」、「複製程式碼」與同層級主要動作使用低彩度、低對比的共用 hover 陰影，不以互補色陰影建立層級。
- 首頁進入第一步與完成動作後進入下一步，都使用一般頁面捲動動畫；reduced-motion 模式改為立即定位。
- 區段切換後可將標題設為程式化焦點以供輔助技術理解位置，但 `tabindex="-1"` 的非互動標題不得顯示瀏覽器文字選取式外框。
- 使用者可在已解鎖區段間捲動或由階段選單返回，但不得由捲動或階段選單越過尚未完成的區段。
- 尚未完成目前區段時，向下捲動的終點須讓當前卡片中心到達或越過畫面中央，並露出下一張模糊卡片的一小段；接近終點時須非線性減速並停在邊界，不得回彈或讓焦點脫離目前區段。
- 第二步完成按鈕第一次觸發時播放點擊動畫，切換為橘色的「再檢查一遍確認沒問題 ↵」待確認狀態，第二次觸發才切換為綠色已確認狀態並前往第三步；第三、四步的完成按鈕第一次觸發即選取並前往下一步。
- 桌面上具有浮起／陰影狀態的按鈕統一使用原生按壓時序：游標按下即呈現下沉，放開後的 `click` 才執行指令；不在指令完成後補播延遲按壓動畫。
- 當焦點不在輸入、選單、連結或其他互動元件，且未處於輸入法組字狀態時，Enter 可觸發目前區段的完成按鈕；實際變更選課或通知設定會重設相關確認狀態並重新鎖定下游步驟。

## 控制臺範本

- 安裝步驟提供 `assets/t-school-control-panel-template.xlsx` 作為 Google Sheets 可匯入的預設控制臺；範本只說明安裝與日常操作，不要求使用者在儲存格中填寫設定。
- 公開 Google Sheets 範本使用 `https://docs.google.com/spreadsheets/d/1MdSMBUNxl8ctdK-q7pO9Sz40Oo-qVPCUKmFssHbs0Ls/copy`，介面直接開啟建立副本流程；若日後清空 `GOOGLE_SHEETS_TEMPLATE_COPY_URL`，則維持下載本機範本的 fallback。

## 更新方式

當目前方向改變時：

1. 直接更新本文件，使其描述「現在採用的方向」，不要累積長篇時間線。
2. 已停用但仍可能需要追溯的規格移至 `archive/`。
3. 若改變涉及長期產品契約、資料模型或安全機制，另同步更新 `PROJECT_CONTEXT_FOR_AI.md`。
4. 若改變形成每次任務都必須遵守的穩定規則，再將精簡版本提升至 `AGENTS.md`。
