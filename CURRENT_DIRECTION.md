# CURRENT_DIRECTION

> 本文件記錄目前階段的產品、UI、互動與設計方向，內容可能隨專案演進而更新。
> 修改相關區域前應閱讀本文件，但不得將歷史方向視為永久架構契約。
> 最近整理：2026-08-11。

## 目前產品重點

- 維持第一次使用者能理解並完成設定的清楚流程。
- 優先提升美感、創新性、T-SCHOOL 品牌辨識與動態方向，再進行嚴格的視覺系統清理。
- 視覺探索不得破壞既有資料載入、驗證、設定碼產生、同步安全與公開 Apps Script 介面。
- UI 文案維持繁體中文、簡潔、以行動為導向，且不假設使用者具備程式背景。

## UI 基準與封存資料

- 在沒有使用者最新要求或更新設計規格時，以根目錄公開設定器目前已實作的介面與元件模式作為後續修改基準。
- 歷史規劃資料位於 `archive/`，除非使用者明確要求，不得作為目前設計依據。
- 先前嚴格的 Material Design 3 規則已封存於 `archive/visual-design-material3/`，目前不啟用。
- `1Campus/` 不得作為視覺或互動參考。

## 目前介面結構

- 安裝器採單一直向敘事流程。
- 設定步驟以自然高度的平面區段由上而下排列；`styles.css` 後段的 flat journey 規則覆蓋早期 sticky 原型，不使用 sticky 卡片堆疊或卡片內嵌捲動。頁面捲動負責整體流程，只有行程清單保留任務需要的局部捲動。
- 非目前區段以 blur 呈現深度；尚未解鎖的唯一下一區段預覽使用五層不同半徑的 `backdrop-filter` 搭配漸層 mask 交錯近似連續變化，霧層與各取樣層橫向超出完整區段以避免卡片或設定碼框側邊產生裁切斷層；已解鎖但非目前區段的卡片統一改用單一全區段霧層。
- 漸進式模糊的整體行程與強度集中由 `--fog-layer-bleed` 與 `--fog-blur-1` 至 `--fog-blur-5` 控制；各層 `--fog-mask` 的漸層百分比用於微調每一階模糊交錯的起訖位置。
- 已解鎖但非目前聚焦的區段（包含卡片下方的完成按鈕）使用單一全區段 `backdrop-filter`；切換焦點時只在 `0px` 與 `--unlocked-section-blur` 之間動畫模糊半徑。這套單層霧化同時用於目前卡片上方與下方的已解鎖區段，避免往回捲動後下方卡片重新啟用五層濾鏡。
- 捲動切換焦點以卡片實際內容區間與畫面中央線的距離判定；中央線仍位於目前卡片內時不切換，離開後另以向上較寬、向下較窄的遲滯距離避免邊界抖動。霧層容器固定存在，離開前後各半個 viewport 觀察範圍的圖層以 `visibility` 停止繪製；只有尚未解鎖的下一張預覽保留五層固定半徑、opacity 交叉淡入淡出的漸進霧化。
- Hero 採左右「文字／紙張轉換」構圖；桌機與手機都由捲動帶動課表、日曆紙張整組由右向左移動，桌機行程較短，並與最後一張行程卡片同時抵達終點。Hero 整段捲動長度集中由 `--hero-scroll-length-desktop` 與 `--hero-scroll-length-mobile` 控制。課表紙張表頭顯示「課程規劃表」與依正式課表學期起日、瀏覽日期計算的目前週次；日曆紙張表頭顯示瀏覽當月及當天的星期、日期。兩張紙張之間以無刻度斜線連接；左右端點分別固定在課表右邊線與日曆左邊線的指定比例，並隨紙張位移及旋轉逐幀重算。紙張與線段位於同一 3D 平面，紙張堆疊在上方並覆蓋線段端部。Hero 開始至結束期間，兩張紙依五次平滑曲線向外分離並略微反向旋轉，相關端點、分離距離、進度區間及旋轉量集中於 `HERO_PAPER_MOTION_CONFIG`。三張行程卡在課表內依序顯示「實體課 [吉林基地]」、「線上課 [線上教室]」與「活動 [弘道基地]」；一離開起點即以動態亂碼取代文字，整張卡片進入起點或日曆紙張後仍依 `heroTileArrivalScrambleDuration`（預設 `400ms`）繼續亂碼，再分別固定為起點文案或「實體課 [吉林基地-協作坊]」、「線上課 [線上教室]」與「活動 [弘道基地-未來教室]」。桌機保留完整功能標籤文案，手機採「標題／紙張動畫／精簡功能標籤／CTA」排列並省略介紹句。手機三張行程卡片的起點與終點分別依課表、日曆紙張的實際位置、表頭高度與列高計算，置於兩張紙張的對應表格列中央，並沿小幅上拱曲線由左往右移動；Hero 高度跟隨 `visualViewport`，CTA 使用正常排版與 safe-area 間距，避免 Safari 底部工具列裁切。紙張與線段共用平面，行程卡則位於前方 Z 深度；桌機以右近左遠的固定透視為基準並隨精細游標小幅轉動，陰影隨視差反向偏移；觸控與 reduced-motion 只保留較弱的靜態透視，不要求裝置方向感測權限。五層水平 progressive blur 跟隨紙張的捲動位移，但維持在 3D 場景之外的畫面平面，不隨游標轉動；圖層高於紙張與行程卡、低於文案與 CTA，且將左側濾鏡邊界延伸至可視範圍之外，上下邊界則以垂直 mask 淡出，避免矩形邊界、Hero 與設定區背景交界閃爍及其他區域色彩滲入。Hero 超出畫面前後 `640px` 後會取消待執行的捲動／3D 動畫幀、停止亂碼更新，並隱藏整個視覺場景以釋放畫面外的繪製與合成工作；重新接近時才恢復圖層並重算版面。手機版因 `.hero-copy` 使用 `display: contents`，標題與功能標籤必須各自建立高於霧層的堆疊層級，不得只依賴已不存在的父層盒子；桌機與手機分別使用 `--hero-fog-desktop-*`、`--hero-fog-mobile-*` 參數。
- 首頁「開始設定」的自動捲動會逐幀傳入兩段線性增加的虛擬捲動目標：先至 Hero 動畫完成位置，再至第一張設定卡片。`MOTION_CONFIG.homeEntryScrollDuration` 控制虛擬輸入總秒數，`homeEntryHeroTimeRatio` 控制 Hero 所占時間比例，預設約為三分之二。按鈕程式不得另加位置 easing 或抵達前減速；畫面的平滑與最後收旂交由 Lenis `scrollLerp` / `scrollTouchLerp` 處理。
- Hero 週次在正式課表完成前，先依已知開學日 114-2（2026-02-23）、115-1（2026-08-31）、115-2（2027-02-11）立即推算；正式來源成功後以來源的學期起日更新，失敗時保留本地推算值，不顯示省略號或破折號。
- 第二至第四步必須按下位於兩張卡片之間的區段完成按鈕，才會解鎖下一步；未解鎖區段不得顯示其完成按鈕輪廓。
- 第一步不預選年級，也不顯示第一、二張卡片間的完成按鈕；選擇年級並成功讀取課表後，直接以動畫前往第二步。
- 課表來源狀態未選年級時保持白色背景；讀取中使用可重用的淺橘色背景 token，左側狀態條與底部進度動畫使用既有橘色色票；讀取成功恢復綠色填色。
- 第一步選擇年級後只由被選按鈕呈現選取回饋，不得在整組三個年級按鈕外圍播放共用綠色光暈或底色。
- 第一至第四步依序採「年級與課表狀態」、「選課程與活動」、「Email／通知偏好」、「可返回修改的設定摘要」架構；步驟 2 主標題與步驟選單精確保留「選課程與活動」，區段內不把整系列的選項稱為單項「行程」。第三步預設開啟「即時通知」：固定課表偵測發現行程調整並完成 Calendar 同步後盡快寄出，每日成功摘要固定於 06:00。網站開關使用與控制臺相同的 44 × 24 px 外框、14 px 滑塊與共用控制元件圓角；說明文字與技術限制提示同層級且可自然換行。關閉後可新增至最多四個不重複通知時段，最後一個時段同時作為每日成功摘要時間。第二步的所有選項共用 `selectedTitles` 單一選擇模型；名稱包含「全校」、「學習分享會」、補假／補課、節假日或模考／模擬考者只套用預設勾選與置底規則，不形成課程／活動類型。只有存在寒暑假項目而形成兩組時，才顯示「學期間課程與活動」與「寒暑假期間課程與活動」小標；只有一組時不顯示小標。各期間先將一般選項依標題相似度排列，再將預設勾選選項依相同方法排列於最後；搜尋只篩選既有順序，不改變設定、指紋或同步語意。第四步以 Hero 同語彙的線框標籤呈現所有設定值，未填警告使用系統橘色；未出現在目前介面的舊篩選、同步預設與進階設定控制不再顯示。
- 第二步搜尋欄有內容時，右側控制改為等視覺重量的取消 icon；點擊或在搜尋欄按 `Escape` 均清除搜尋並恢復完整清單，輸入法組字期間不得誤觸。
- 步驟標題、步驟位置、選擇按鈕、圖示按鈕與設定摘要列優先以共用元件模式維護，並統一卡片內按鈕的 hover、active 與 reduced-motion 狀態。
- 選擇型按鈕的桌面 hover 僅使用中性描邊與低對比陰影，與綠色填色及綠色位移陰影的已選狀態明確區隔；無 hover 或粗略指標裝置取消 hover 視覺，只保留瞬時 `:active` 按壓回饋與 `:focus-visible` 鍵盤焦點。
- 設定碼輸出區段與設定步驟分離。
- 第五步採深色「變出控制臺」卡片：設定碼不隨設定即時重建，只在第四步按下「產生安裝設定碼」時產生或更新；完成後先保留一個短暫的瀏覽器繪製間隔，再開始第四至第五步的切換。第五張預覽與進場動畫沿用既有透明度與模糊參數。設定碼表面以獨立、全區域 `backdrop-filter` 模糊遮罩保護內容，底部仍保留純黑透明度漸層；不建立內嵌捲動，也不得以 `data-lenis-prevent` 排除頁面的 Lenis 捲動。桌機主要動作為「複製設定碼」，下方以可再次點擊複製的「複製設定碼」作為第一步，再依序顯示登入設定 Email、建立 Google Docs 母版副本與依畫面完成設定，共四步；手機、行動 user-agent 或只有粗指標的裝置改為「寄送設定信」，下方只提示到電腦收信，不顯示母版連結或額外複製按鈕。原生寄信選單以「設定指引｜T-SCHOOL Schedule Sync」為主旨，內文依序提供完整設定碼、使用設定 Email 建立母版副本與完成設定指引；內容超過安全 URL 長度時改用系統檔案分享，無法分享時下載純文字設定檔。
- 第五步「接下來怎麼做」的步驟一點擊後仍固定顯示「複製設定碼」，只由 contextual cursor 顯示成功回饋；行內動作使用文字原生底線，不以 `border-bottom` 模擬底線。
- 卡片間的連接線直接接觸上下卡片；當前卡片下方的完成按鈕保持清楚並位於遮罩上方，當前卡片上方已使用過的按鈕則納入 progressive blur，只有接至當前卡片的下段線條逐漸從霧中變清楚。
- 使用 contextual cursor motion 輔助呈現流程；游標本體主要指向必須與 macOS 原生游標的微幅左傾一致，基準角度與擺動幅度集中於 `MOTION_CONFIG.cursorBaseAngle`、`cursorSwayMaxAngle`、`cursorSwayVelocityScale` 與 `cursorSwayReturn`。移動時只可在主要方向兩側微幅擺動，靜止後回到原生指向，不得完全朝運動方向旋轉。游標以逐像素灰階與反相在背景邊界同時呈現深色／淺色，外圍只保留一圈半徑約 11 px、較細、較淺且隨移動速度輕微伸縮的光圈，不產生鮮豔互補色，且動畫不得阻礙任務完成。游標標籤靠近 viewport 邊緣時必須自動限制水平位置或翻到游標上方，完整保留在畫面內。右上角步驟選單按鈕的 hover 不顯示外框或底色，只由游標標籤依狀態顯示「開啟步驟選單」或「關閉步驟選單」；鍵盤 `focus-visible` 提示仍保留。複製設定碼成功時，不顯示右下角通知，改由目前複製媒介的游標補充標籤以綠底白字顯示「複製完成！」。
- 觸控操作與 reduced-motion fallback 必須保持可用。
- 行動裝置聚焦輸入欄位時，以 `visualViewport.height` 與 layout viewport 的高度差穩定判斷鍵盤狀態，不將會隨 caret 移動的 `offsetTop` 當成鍵盤高度。由完成按鈕程式化聚焦 Email 時不得使用 `preventScroll`，應讓瀏覽器在鍵盤開啟前先把欄位移入可視範圍；鍵盤開啟後只暫時放寬目前步驟的捲動邊界，並交由瀏覽器維持輸入游標可見，不再以 Lenis 或 `window.scrollTo` 二次校正；不得解鎖或切換至下一步。鍵盤收起、裝置旋轉或分割畫面改變後重新計算版面並恢復一般卡片邊界；卡片維持自然頁面捲動，不增加卡片內嵌捲動。
- 第三步 Email 驗證失敗時使用卡片內提示、`aria-invalid` 與自動聚焦，不呼叫瀏覽器原生 constraint-validation 浮窗；每張 `.journey-step` 建立獨立 Backdrop Root，唯一可見預覽的五個 `backdrop-filter` 圖層保持為穩定合成層。Email 的紅／綠外框、圖示與提示文字預先保留於固定合成層，驗證時只切換透明度，不替換文字或重繪 input border，避免相鄰預覽濾鏡被 Chromium 暫時撤除再重建而產生單幀閃爍。
- 行動裝置使用 Lenis `syncTouch` 的逐幀 `lerp` 平滑。Safari 鍵盤與候選字列動畫期間先合併連續的 `visualViewport` 事件，輸入期間的 offset 或高度變動只更新邊界，不呼叫 imperative scroll；只有寬度／方向改變或鍵盤關閉後才重算流程幾何。輸入期間停用 scroll anchoring，root scroll padding 不重複加入鍵盤高度；搜尋結果或其他內容高度改變只更新流程幾何，不得再次觸發欄位捲動校正。
- Google Docs 設定側欄延續安裝器的四色調色盤、緊湊圓角、4 px 間距網格、線條式區段層級與響應式動態語言；不得恢復封存的 Material 3 卡片樣式。上方自訂選單固定命名為「T-SCHOOL Schedule Sync」，並使用「開啟控制臺介面」與「關閉 / 啟用自動同步」等一致文案。首次匯入使用單頁對話框，貼上「行程同步設定碼」後直接重新驗證並開啟控制臺，不顯示摘要確認或來源變動警示。能取得目前 Google 帳號且與設定 Email 不一致時，在同一貼上頁顯示「用錯 Google 帳號了喔，請改成設定時填寫的 {Email}」並停止保存；無法取得帳號時不推測。主要設定區段與同步行為沿用既有側欄；首次同步前顯示「重新匯入網站設定碼」，完成後隱藏並由後端拒絕覆寫。
- 通知 Email 只保留一套標準版型；所有連回綁定文件的按鈕統一顯示「開啟行程同步控制臺」，且只允許 `calendar.google.com/calendar/` 與 `docs.google.com/document/` 連結。其餘狀態色、首次設定通知、課綱索引差異與純文字備援規則維持不變。
- 新學期 30 分鐘驗證期間，Google Docs 控制臺使用現有珊瑚色需注意狀態顯示靜態警示，程式化將焦點移至警示，並同時停用儲存、同步與強制修復。不加入循環動畫；`prefers-reduced-motion` 下保留完整內容、焦點與操作順序。

## 設計系統維護

- 根目錄 `styles.css` 是公開設定器的實作來源；`CURRENT_DIRECTION.md` 記錄其跨頁面規則，兩者不一致時應先以最新 UI 查核，再更新本節。
- 公開設定器使用低彩度紙張綠為背景（`--paper: #f4f7f2`）、白色表面、深墨綠文字（`--ink: #14211d`）、同步綠（`--sync: #00a676`／`--sync-dark: #007c59`）與調整珊瑚色（`--shift: #f05a47`）。一般狀態不得另增無語意的強調色。
- 字體以 `Noto Sans TC Variable`／`Noto Sans TC` 為 display、body 與 technical 共同基礎；只有設定碼等真正的程式內容使用 monospace。公開設定器的主要字級與行高由 `--text-*`／`--leading-*` token 管理。
- 共用尺寸以 4 px 網格為基礎：`--space-1` 至 `--space-24`、公開設定器卡片圓角 `8px`、控制元件圓角 `4px`、主要控制高度 `48px`。控制臺因可用寬度較小，使用 `3px` 控制圓角、`4px` panel 圓角與相同 4 px 間距倍數。
- 狀態介面沿用「窄色條＋紙張表面＋標題／說明」模式：綠色表示可繼續，珊瑚色表示需要留意或確認，深紅只表示不可繼續的錯誤；loading 可使用淺珊瑚表面與同色進度，不以紅色錯誤語氣呈現。需要使用者決定的狀態必須提供就近、具體的動作，不只靠 toast。
- 選取狀態以綠色表面、深色描邊與短距離位移陰影呈現；一般 hover 使用中性或低對比綠色表面，不能與已選狀態混淆。按下採瞬時下沉，`focus-visible` 使用同步綠外框；觸控目標至少 `44 × 44px`。
- Google Docs 側欄、首次匯入對話框與公開設定器共用上述四色語意、字體、4 px 網格、緊湊圓角與狀態模式。側欄可採較小字級與較密間距，但不得回到 Arial 預設外觀、Material 3 大圓角卡片或另造警告配色。
- Google Docs 側欄顯示同步進度時，只保留進度標題、百分比、進度條與目前階段，不在上方重複顯示「正在同步行程」。真正開始同步前的準備階段由 0% 起始，並在階段文案後標示固定的可能等待時間，不使用執行期間動態推算。
- 動態只用於說明狀態與流程：狀態切換採短距離 opacity／transform，阻擋或確認資訊不加入循環動畫；`prefers-reduced-motion` 時移除非必要轉場但保留完整內容、焦點與操作順序。
- 在可行時，先調整共用 token，再新增元件專用的孤立數值。
- 主觀美感、層級、節奏、對齊、對比、響應式構圖與品牌一致性由使用者進行最終判斷。

## 動態與捲動

- 主要動態參數集中於根目錄 `app.js` 的 `MOTION_CONFIG`；優先修改集中設定，避免在事件處理器中加入零散常數。
- 平滑頁面捲動由同檔案頂部的 `ENABLE_SMOOTH_SCROLL` 開關；設為 `false` 即可停用 Lenis 與程式觸發的平滑捲動，reduced-motion 偏好仍優先停用。
- `MOTION_CONFIG` 控制區段解鎖轉場、手動捲動的 `lerp`、鎖定邊界的回穩時間、鍵盤 viewport 穩定等待時間、active section activation line 與首頁進入第一步的捲動時間。
- 手動滾輪、觸控板與觸控捲動使用 Lenis 的逐幀 `lerp`；不得在 Lenis 建構設定加入全域 `duration`，避免瀏覽器漏幀後依經過時間突然追趕。完成按鈕、階段選單、回到頁首及鎖定步驟邊界仍可各自指定 `duration`。
- Lenis 使用內建 `autoRaf` 驅動手動與程式捲動，不另以事件喚醒、`isScrolling` 判定休眠的自訂 RAF 迴圈，避免卡片狀態更新或文件邊界讓動畫在臨界幀提前停止。
- 所有步驟解鎖後，頁面只使用 Lenis 的文件邊界與連續動量，不另設頁尾攔截，也不因單次極小反向輸入立即重設捲動目標。尚未解鎖下一步時則只將最終目標封頂於目前流程邊界，不縮小每次手勢的輸入距離。
- 手動捲動手感優先調整 `scrollLerp`、`scrollTouchLerp`、`scrollWheelMultiplier` 與 `scrollTouchMultiplier`；鎖定步驟的一般邊界收斂調整 `boundarySettleLerp`。從 Hero 快速滑向第一個鎖定邊界時，若 Lenis 目標位置明顯領先畫面，另以 `initialBoundaryMinLerp` 與 `initialBoundaryBlendDistanceRatio` 混合成較柔和的收斂速度；程式捲動則調整 `sectionTransitionDuration`、`homeEntryScrollDuration` 與 `footerReturnScrollDuration`。
- 自訂游標只使用深色與淺色兩種狀態，依游標下方背景明度切換；保留輕微跟隨延遲，但不得使用互補色反相。
- 「開始設定」、「複製設定碼」與同層級主要動作使用低彩度、低對比的共用 hover 陰影，不以互補色陰影建立層級。
- 首頁進入第一步與完成動作後進入下一步，都使用一般頁面捲動動畫；reduced-motion 模式改為立即定位。
- 區段切換後可將標題設為程式化焦點以供輔助技術理解位置，但 `tabindex="-1"` 的非互動標題不得顯示瀏覽器文字選取式外框。
- 使用者可在已解鎖區段間捲動或由階段選單返回，但不得由捲動或階段選單越過尚未完成的區段。
- 尚未完成目前區段時，向下捲動的終點須讓當前卡片中心到達或越過畫面中央，並露出下一張模糊卡片的一小段；接近終點時須非線性減速並停在邊界，不得回彈或讓焦點脫離目前區段。
- 第二步完成按鈕第一次觸發時播放點擊動畫，切換為橘色的「再檢查一遍確認沒問題 ↵」待確認狀態，第二次觸發才切換為綠色已確認狀態並前往第三步；第三、四步的完成按鈕第一次觸發即選取並前往下一步。
- 桌面上具有浮起／陰影狀態的按鈕統一使用原生按壓時序：游標按下即呈現下沉，放開後的 `click` 才執行指令；不在指令完成後補播延遲按壓動畫。
- 當焦點不在輸入、選單、連結或其他互動元件，且未處於輸入法組字狀態時，Enter 可觸發目前區段的完成按鈕；實際變更行程選擇或通知設定會重設相關確認狀態並重新鎖定下游步驟。

## 控制臺母版

- 公開安裝流程使用已預載通用 Apps Script 的 Google Docs 母版 `/copy` URL，不提供 `.xlsx` fallback。
- 正式 URL 為 `https://docs.google.com/document/d/1l4SCo0Z8cDgy1F0wvc74exuBIES4MHRO8F08GiqqZgA/copy`，由 `app.js` 的 `GOOGLE_DOCS_TEMPLATE_COPY_URL` 管理；值為空或格式不是 `https://docs.google.com/document/d/.../copy` 時，網站必須顯示準備中並停用連結。
- 母版發布、OAuth 與腳本隨副本複製的驗證需在 Google 雲端以全新 Docs 副本另行完成。

## 更新方式

當目前方向改變時：

1. 直接更新本文件，使其描述「現在採用的方向」，不要累積長篇時間線。
2. 已停用但仍可能需要追溯的規格移至 `archive/`。
3. 若改變涉及長期產品契約、資料模型或安全機制，另同步更新 `PROJECT_CONTEXT_FOR_AI.md`。
4. 若改變形成每次任務都必須遵守的穩定規則，再將精簡版本提升至 `AGENTS.md`。
