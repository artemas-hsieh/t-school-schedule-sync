# T-SCHOOL 課表同步專案指引

## 工作語言

- 對使用者的溝通、變更摘要與使用者介面文案，預設使用繁體中文；使用者另有要求時除外。
- 函式名稱、變數名稱、檔案路徑、指令、API 與程式識別名稱保留英文。
- 回報應簡短、具體，先說明結果，再說明驗證與限制。

## 專案概覽

本專案提供免建置、無外部執行階段依賴的靜態設定產生器，產生由使用者自行部署的 Google Apps Script `Code.gs`。產生的程式會讀取 T-SCHOOL 課程與活動規劃表、同步選定事件至專用 Google Calendar，並在綁定的 Google Sheet 中提供設定側欄。

## 重要路徑

- 根目錄 `index.html`、`styles.css`、`app.js`、`schedule-data.js`、`sidebar-template.js`、`code-template.js`：公開設定介面、資料處理與 Apps Script 程式碼產生邏輯。
- `tests/`：自動測試與冒煙測試。
- `archive/`：封存資料；除非使用者明確要求，不得將其作為目前實作依據。
- `PROJECT_CONTEXT_FOR_AI.md`：相對穩定的產品流程、架構、同步模型與安全背景。
- `CURRENT_DIRECTION.md`：目前階段的產品、UI、互動與設計方向。
- `TESTING.md`：完整驗證範圍、指令與人工檢查方式。

## 文件讀取規則

不要為每個小型任務完整讀取所有文件，依變更範圍選擇：

- 修改產品流程、資料解析、Apps Script 產生、Calendar 同步、通知、狀態遷移、安全機制或公開函式前，閱讀 `PROJECT_CONTEXT_FOR_AI.md` 的相關章節。
- 修改 UI 視覺、互動、動畫、響應式行為或設計系統前，閱讀 `CURRENT_DIRECTION.md`。
- 執行或規劃測試時，依 `TESTING.md` 選擇最小但充分的驗證。
- 單純修正文案、局部樣式或孤立錯誤時，只需檢查受影響檔案與直接相關規則。
- 若文件、程式碼、測試或使用者最新要求彼此不一致，當前使用者要求優先；不得默默猜測其餘衝突，應在完成回報中指出。

## 核心實作限制

- 公開設定產生器必須維持靜態、免建置、無外部執行階段依賴；架構變更須由使用者明確同意。
- 在沒有更高優先級的新需求或設計規格時，以根目錄公開設定器的目前實作作為 UI 行為與視覺基準。
- 保留既有資料解析、Calendar 安全規則、產生的 Apps Script 行為、設定遷移與公開 Apps Script 函式；除非任務明確要求並規劃相容性處理。
- 使用正式 Apps Script `/exec` 課表端點；不得保存重新導向後的 `script.googleusercontent.com` 網址、權杖或 `user_content_key`。實際端點以程式碼中的正式設定為準。
- 產生 Apps Script 時，使用者提供的值必須安全序列化；不得將使用者字串直接串接成可執行程式碼。
- 優先維持 Calendar 配額安全、可復原性與資料保護，不得為追求速度削弱安全機制。
- 繁體中文 UI 文案應簡潔、以行動為導向，且不假設使用者具有程式背景。

## 修改原則

- 修改前先檢查目前差異，只變更本次任務必要的檔案與區段。
- 保留無關的未提交變更，不得在未獲明確指示時還原、覆寫或刪除使用者的修改。
- 新增函式、設定欄位、狀態來源或 UI 模式前，先搜尋是否已有相近實作。
- 優先延伸既有模式，避免重複工具函式、平行狀態來源或第二套設定格式。
- 除非任務需要，不進行與目標無關的大規模重構。
- 不得自行 commit 或 push；使用者明確要求時才執行。

## 視覺驗證邊界

- 預設不進行耗時的逐像素比對、密集截圖迭代或大量 viewport 重複測試，除非使用者明確要求自動化視覺審查。
- 可用低成本方式確認的明顯版面破損仍應檢查，包括水平溢出、裁切、遮擋、缺少元素、瀏覽器錯誤及明顯響應式失效。
- 導航、焦點、鍵盤操作、狀態轉換、表單行為、資料流與 reduced-motion fallback 屬於功能正確性，不得視為純視覺問題而略過。
- 美感、視覺層級、間距節奏、對齊、對比、動態節奏、響應式構圖與品牌一致性由使用者進行最終判斷；完成時列出仍需目視確認的項目。

## 驗證

依變更範圍執行 `TESTING.md` 所列的最小但充分驗證。最低要求：

- 所有程式碼變更：執行 `git diff --check`。
- JavaScript 變更：對受影響檔案執行 `node --check <file>`。
- 影響資料解析、設定、產生程式碼或共用應用行為：執行 `node tests/smoke-test.js`。
- UI 互動變更：測試受影響的互動、鍵盤操作、焦點狀態與 reduced-motion 行為。

## 完成回報

完成任務後，簡要說明：

- 修改了什麼。
- 執行了哪些驗證及結果。
- 哪些部分未能驗證或仍需使用者目視確認。
