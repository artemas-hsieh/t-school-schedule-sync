# PROJECT_CONTEXT_FOR_AI

> 本文件說明 T-SCHOOL 課表同步專案相對穩定的產品與架構背景，供涉及相關區域的任務按需查閱。
> 它不取代 `AGENTS.md`，也不是每項任務都必須完整載入的操作指令。
> 目前階段的 UI、互動與產品方向以 `CURRENT_DIRECTION.md` 為準；驗證方式以 `TESTING.md` 為準。
> 若本文件與程式碼、測試或使用者最新要求不一致，應指出差異，不得默認採用較舊內容。

## 專案概覽

本儲存庫包含一個靜態安裝設定器。網站產生版本化設定碼，使用者將設定碼匯入已預載通用 Apps Script 的 Google Docs 母版副本；通用程式會讀取課綱 Sheets、同步至使用者選擇且自行擁有的 Google Calendar，並在綁定的 Google Docs 中提供設定側欄。

公開應用維持免建置，GitHub Pages 根網址直接載入設定器：

- 根目錄 `index.html` 定義安裝器 UI。
- 根目錄 `styles.css` 實作設計系統。
- 根目錄 `schedule-data.js` 讀取公開課程索引，供安裝器產生課程選項。
- 根目錄 `setup-code.js` 定義網站與 Apps Script 共用的設定碼格式。
- 根目錄 `sidebar-template.js` 與 `setup-dialog-template.js` 分別包含設定側欄與首次匯入對話框。
- 根目錄 `code-template.js` 產生 Google Docs 母版使用的通用 Apps Script 後端。
- `scripts/generate-google-docs-control-panel.js` 將後端與兩個 HTML 介面合成忽略版控的 `outputs/google-docs-control-panel/Code.gs`，並同時產生明確限制權限的 `appsscript.json`。
- 根目錄 `notification-email-templates.json` 是所有通知共用的公開 HTML Email 版型 manifest。
- 根目錄 `app.js` 協調安裝器狀態、驗證、課程索引載入、行程選擇與設定碼產生。

目前公開部署位置：

```text
https://artemas-hsieh.github.io/t-school-schedule-sync/
```

## 執行階段資料來源

2026/09/19 起，網站讀取公開課程索引的年級分頁；Apps Script 透過 Sheets v4 唯讀服務讀取中央課綱來源索引與已啟用課綱，一般課程日期、節次、地點與內容直接來自課綱；週四／週五跨校選修依固定第 3–4 節規則，於該年級課綱實際起訖內產生，排除官方國定假日及補假

完整欄位、學期啟用、解析保護與更新流程見 [課綱來源指引](docs/sheet-source.md)，共用解析與讀取實作在 `sheet-source.js`

正式執行不再要求公開課表 API，也不使用私人本機擷取、全校活動或課表備註；舊解析與正規化契約函式只保留為相容及測試輔助

中央索引和課綱讀取失敗時保留既有日曆；最後成功摘要只供控制臺唯讀顯示，不能授權 Calendar 寫入或證明學期結束

## 產品流程

1. 網站讀取公開課程索引，依「課程名稱」「課程類別」顯示選項，必修預設勾選
2. 使用者選課並設定 Email／通知；產生 `TSCHOOL_SETUP_V1` 設定碼，schema 2 增加 `sourceKind: course-index`，不在網站宣稱已驗證實際學期
3. 使用者以校內 Google 帳號建立 Docs 副本並授權，匯入時讀取實際課綱，驗證課程名稱及可設定學期；匯入只保存設定，不建立 Calendar 事件
4. 控制臺初次顯示使用已驗證的小型來源摘要；首次同步前仍重新讀取全部當期課綱
5. 使用者選定自有 Calendar 並同步；日期、地點及課綱內容一起進入既有可續傳批次工作，完成後啟用固定同步與通知排程
6. 來源失敗時控制臺可顯示最後成功的唯讀摘要，停用寫入，不將快取作為正式來源

網站設定可在行動裝置完成，手機以「寄送設定信」傳送設定碼至自己；建立 Docs 副本、匯入與控制臺操作仍需電腦

保留 Google 帳號 Email 比對、首次同步後禁止重新匯入、日曆所有權檢查及既有通知偏好

## 資料解析與行程目錄

- 課程名稱以課綱工作表分頁名稱為準，網站列表必須使用同一名稱，不改用表內中文名稱或模糊推測
- 課程索引的「課程類別」只決定初始勾選，所有選擇仍使用 `selectedTitles`，後續不覆寫使用者勾選
- `自然進階(二)` 現在直接顯示並可選；舊分科選擇在第一次改用課綱來源時映射至這個實際分頁
- 日期依來源組範圍辨識唯一月日，支援跨年舊年份、合併儲存格、明確跨日範圍與連續／不連續節次，具體規則及不能判定時的保護見來源指引
- 純非同步列不產生 Calendar 時段；有日期節次的課程使用固定第 1–8 節鐘點
- 主同步掃描該年級完整學期，使用者未選課程也納入學期結束判定
- 完整來源才允許舊版非課程未來事件進入移除計畫，過去事件與其他控制臺／私人事件不變
- 目錄、整份時段內容、首次設定摘要及同步工作單維持不同指紋資料域；學期識別與來源組鍵不由今日日期硬編碼
- schema 9 舊選擇與指紋欄位的既有單向遷移保留

## 產生的 Apps Script 公開介面

除非已規劃明確遷移，以下公開函式應保持穩定：

- `syncMyScheduleToCalendar()`
- `syncMyScheduleToCalendarWithNotification()`
- `forceFullSyncMyScheduleToCalendar()`
- `setupAutoSyncTriggers()`
- `deleteAutoSyncTriggers()`
- `quickDeleteAllCalendarEvents()`
- `quickDeleteSyncedCalendarEvents()`
- `resetSyncState()`
- `previewParsedEvents()`

綁定的 Google Docs 另公開 `onOpen()`、`showSettingsSidebar()`、`showSetupImportDialog()`、`importSetupCodeFromUi(code)`，並保留相容用的 `previewSetupCodeForUi(code)`、`applySetupCodeFromUi(code, confirmationToken)`、狀態與選單操作，以及側欄使用的 `google.script.run` handlers。容器 UI 與 URL 一律經 `getControlPanelUi_()`／`getControlPanelUrl_()` 使用 `DocumentApp`；控制臺初次呈現只讀既有課綱索引快照或內建 fallback，不同步開啟中央索引試算表。正式課綱更新使用 manifest 啟用的 Sheets v4 進階服務與 `spreadsheets.readonly` scope；不得改用只接受完整 Sheets scope 的 `SpreadsheetApp.openById()`。

## Calendar 同步模型

- 使用者從控制臺建立專用 Calendar，或首次同步自動建立 Calendar 時，一律以 `selected: true` 建立，使新 Calendar 預設顯示在 Google Calendar 介面中。
- 只主動協調今天與未來事件；過去的 Calendar 行程不改不刪，內部索引則只保留最近 120 天，以免長期占滿 Script Properties。
- 第 1–8 節固定對應 `08:25–09:15`、`09:15–10:05`、`10:15–11:05`、`11:05–11:55`、`13:25–14:15`、`14:15–15:05`、`15:15–16:05`、`16:05–16:55`；不讀取來源左欄鐘點。跨節行程取首節開始與末節結束建立單一連續區間，不因下課或午休拆分。比對前將同日期、同正規化標題、同課表基地且重疊或相鄰的非全天事件合併。
- 正式同步採可續傳工作單：首次第一批最多 40 次 Calendar 操作，後續每批最多 80 次；每批 150 秒軟上限，批次前保存 inFlight、批次後提交狀態；第一批安全完成後只寄一次設定完成通知
- 每份控制臺在 Script Properties 建立一個綁定目前 Apps Script 專案 ID 的隨機事件權杖；新事件以「管理標記＋同步識別碼＋控制臺權杖」三組隱藏標籤辨識。建立工作續跑時只接回三者完整相符的事件；沒有完整標籤的事件即使標題、時間、地點與說明完全相同也一律不接管。若 Calendar 建立成功但在完整寫入標籤前中斷，安全上允許下一次建立重複事件，不得改用內容相同作為接管依據。
- 每次正式同步會先以一次日期範圍讀取檢查今天與未來、仍在目標課表中的受管理事件是否還存在。只有同步狀態中保存的 Event ID，或三組隱藏標籤完整相符的替代事件，才能參與判定；同日曆中使用者手動建立的同名事件不算。若 Event ID 回傳明確的不存在／已刪除，系統先重建事件與同步狀態，寄出主旨為「偵測到行程被刪除｜T-SCHOOL Schedule Sync」的提醒，並在控制臺「同步狀態」標題下方、上次同步指標之前列出待確認項目。信件內標題固定為「偵測到行程被刪除」，補回原因與前往控制臺確認的說明都位於行程列表上方；Email 只連回控制臺，不在信件內執行 Calendar 寫入。
- 待確認項目提供「刪除單一事件」與「刪除整門課程」。單一刪除會直接將補回的事件移至 Calendar 垃圾桶，並以「學期＋occurrence key」保存排除，後續同步不再補回這一筆；整門刪除會先要求確認，再移除今天以後同標題的受管理事件，從 `selectedTitles` 移除並加入 `excludedTitles`。兩者都只能依已保存狀態與完整管理標記刪除，不掃描或改動私人事件。學期轉換、重設同步狀態或「移除受管理事件」時一併清除這些待確認與單次排除。
- 日期改動配對會先依「正規化行程標題＋正規化課表基地」分桶，不同基地永遠不得互相配成調整。同標題可合法同時存在於吉林基地、線上教室或弘道基地，但各自只尋找同基地舊事件。配對另限定全天型態相同、節數長度相同，避免把調課判成跨節事件擴張、縮短、拆分或合併。若新舊事件都具有課綱「單元主題＋課程內容」組合身分（至少一欄有內容），兩者必須相同；相同身分優先於時間距離，實體課程教室不納入身分。缺少課綱身分時，才沿用同標題、同基地、同節數、21 日內唯一最近的貪婪配對；等距時仍不得任選，節數不同則維持分開的新增與取消。配對成功後保留原本完整的起訖節次，不得拆成單節或跨越不同配對互相抵銷。
- 設定儲存與同步共用 Script Lock；未完成的 Calendar 搬移結束前禁止再次更換目標，避免雙分頁或連續搬移破壞狀態。
- 所有同步入口都必須遵守新學期候選與待重新選課的寫入限制，既有 Script Lock 與觸發器互斥規則保持不變
- 指紋契約按資料域分離：`catalogFingerprint` 只保護安裝選項目錄，`scheduleFingerprint` 保護完整課綱行程，`setupContextFingerprint` 保護首次同步前的內嵌設定快照，同步工作單的 `desiredScheduleFingerprint` 則只保護篩選後的目標事件。跨網站與 Apps Script 的目錄列使用明確的字典序正規化，不依賴執行環境的 `localeCompare` 預設值。每次續傳均比對目標行程、設定、目標 Calendar、課綱版本與應有事件指紋。輸入漂移時由已提交狀態重新規劃，不沿用過時操作清單。
- 主同步第一次暫時失敗保存進度並重試一次，下一次仍失敗才寄信；watchdog 亦採相同的連續兩次失敗規則。每次新批次開始都先刪除舊 watchdog 再建立完整 5 分鐘的新觸發器，不能沿用前一批殘留時間。
- 分塊 Script Properties 以 `<key>_COUNT` 作為目前值的提交指標。一般寫入不得刪除超出目前 count 的歷史尾端 chunk，以免舊的小寫入與新的大寫入交錯時破壞 JSON；只有明確清除整個 store 時才掃描並移除所有數字 chunk。需要讀改寫語意的設定仍由上層 Script Lock 保護。
- 課表偵測與 Calendar 同步以 03:00、11:00、18:00、21:00 為四個中心，依每份 Apps Script 副本的固定識別分配 Trigger 中心分鐘，再保留 Apps Script `nearMinute` 約 ±15 分鐘誤差，確保實際執行落在各中心前後一小時內。重新建立 Trigger 時同一副本維持相同分配，且完全不受通知偏好影響。即時通知開啟時，異動在當次同步完成後直接嘗試寄送，並另建立 06:00、`nearMinute(0)` 的通知 Trigger，只在週日無異動時寄出成功摘要；關閉時才依使用者選擇的一至四個時間建立同精度的獨立通知 Trigger。通知 Trigger 只寄送佇列內容，不重新讀取課表或更新 Calendar。課綱時段與內容已整合至主同步，不再建立獨立每日補充排程。
- 中央課綱索引依標頭定位欄位並驗證啟用值、來源組、年級、起訖日期與一般 Google Sheets `/edit` 連結；同一來源組的列必須使用相同年級與日期，試算表不得重複。
- 課綱中的 `實體課程教室` 會與課表地點去重後以 `-` 組合，寫入事件標題右側的方括號及 Calendar 地點；`單元主題`、`課程內容` 寫入說明。垂直合併的資料儲存格會把左上值套用到合併範圍內各列。純課綱變更會更新這三個呈現欄位，但不列入行程調整通知。
- 側欄不再提供說明格式選項；新安裝固定使用標準格式。既有安裝若已保存進階自訂模板，儲存其他側欄設定時仍保留原值，以避免無預警改寫既有事件內容。
- 來源完全未變更的事件會跳過個別 Calendar 內容讀取與寫入；每次同步仍會作一次批次存在性檢查，以偵測受管理事件是否被手動刪除。
- 一般同步在來源 signature 未變更時保留使用者直接對 Calendar 做的修改；強制修復才重新套用來源欄位。
- 來源更新標籤不納入同步 signature，避免無實質內容變化的更新時間造成大量 Calendar 寫入。
- 明確的同標題、同基地、同節數日期或節次變更，會在 21 天範圍內配對為調整；課綱的「單元主題＋課程內容」組合可作為同基地內的事件身分，其中一欄留空時仍使用另一欄已提供的資訊。節數不同、課綱身分明確衝突、基地不同或其他不明確情況維持分開的新增與取消；一節配一節、連續多節配相同長度的連續多節，完成配對後不再拆分。
- 偵測到可疑的大量刪除時，自動同步與強制修復會停止。設定變更只有在實際寫入前仍符合剛才預覽所產生的確認 token 時才能套用大量刪除；來源或設定改變後 token 即失效。
- 切換 Calendar 時，先在新的同步目標 Calendar 重建事件，再移除舊 Calendar 中由同一控制臺管理的事件。
- 舊版 `SYNC_STATE` 會遷移至分塊儲存。舊版受管理事件 fallback 必須同時具備管理標記、A1 格式來源儲存格與原始內容文字；今天與未來事件會逐批改用隱藏標籤並套用新版顯示格式。

## 通知與學期轉換

- 來源變更會寄送一份標準「行程調整」摘要，異動類型只有調整、新增與取消，並顯示日期、節次、固定節次時間、課表基地與標題；純課綱教室或內容變更不納入通知。
- 即時通知開啟時，固定同步偵測到的行程調整會先合併並持久保存，Calendar 同步完成後立即嘗試寄出；寄送失敗時保留佇列並排程重試。關閉時，異動持續保存到下一個使用者通知時間成功寄出，後續無異動同步不得清除。通知 Trigger 若與仍在執行的同步重疊，會延後重試；既有的一次性通知重試不得因每日排程重整或關閉後續自動同步而被刪除，必須先完成已保存的待寄請求。同步錯誤、課綱錯誤與「行程同步設定完成」通知可立即寄發。
- 每週日最多寄一封成功摘要：即時通知開啟時固定 06:00，關閉時安排在最後一個自訂通知時間。當日已偵測、排程或寄出行程調整時，不再寄成功摘要。
- 所有通知同時提供 HTML 與純文字內容；純文字內容是手機推播摘要與不支援 HTML 的備援。產生的 `Code.gs` 只保存指向已核准 commit 的不可變 HTTPS manifest URL 與安全渲染器，不內嵌信件 HTML，也不追蹤主分支即時內容；同一固定版本以一小時 Script Cache 快取，並在同一次 Apps Script 執行中只讀取、解析一次成功版型。版型大小上限依 UTF-8 位元組計算；下載、大小或驗證失敗時仍須寄出純文字。
- 所有通知主旨統一為「通知事件｜T-SCHOOL Schedule Sync」；首次設定只寄一次「行程同步設定完成」，行程調整使用「有 n 項行程調整」，每週日成功摘要使用「行程同步狀態正常」。
- 控制臺網址在互動操作時以目前文件 ID 正規化並存於 Script Properties，綁定 Script ID 防止副本連回母版；背景通知沿用同專案網址，佇列內容不可覆寫此網址，純文字也附網址，無可信網址時不顯示無效 HTML 按鈕
- HTML manifest 中的一般 `{{value}}` 一律經過跳脫；只有程式從同一份 manifest 重複渲染出的區塊可透過 `{{{value}}}` 插入，使用者或課表來源文字不得直接成為 HTML。
- 完整 HTML 渲染後只允許 `calendar.google.com/calendar/` 與 `docs.google.com/document/` 的 HTTPS 連結；不符合白名單的 `<a>` 會移除，只保留其中的文字。
- 權限、資料結構、名稱碰撞、大量刪除等需處理的錯誤應立即通知；暫時性的 Google 服務錯誤或逾時先延後重試一次，下一次仍失敗才通知。Calendar 或設定已提交後，控制臺重載、狀態顯示、觸發器整理或通知後處理失敗只能回報「主要操作已完成」的警告，不得寄出或彈出「行程同步失敗」。
- 無變更執行預設不通知，但週日的成功摘要時段除外。
- 事件提醒預設關閉，可由使用者設定。
- 學期識別固定為「課表年級｜學年度-學期」，8–12 月開始屬當年度第 1 學期，1 月仍屬前一年度第 1 學期，2–7 月屬前一年度第 2 學期；例如 `二年級|2026-1`。舊版「年級｜首日」識別在讀取時轉為同一格式，v2 目錄與設定摘要指紋仍可驗證；新產生的目錄、設定摘要與完整課表指紋使用 v3。
- 只有當前年級全部課程結束且緊接的新學期來源已啟用、完整可讀才建立候選；曾讀取的最後結束時間也是防止來源縮短的下限。候選期凍結 Calendar，同一新學期及內容穩定 30 分鐘後才要求重新選課；返回原學期則清除候選，來源失敗不能完成確認
- 確認新學期後，系統保持 Calendar 凍結、暫停自動同步、保留既有 Calendar 事件，並把 `selectedTitles` 重設為新學期的預設選項供使用者確認；只有使用者完成年級確認與重新選課後才恢復同步。新學期 Email 由專用一次性觸發器在通知時段寄送：即時通知模式使用下一個 06:00，關閉即時通知時使用下一個自訂時段。處理器執行時先清除已消耗的觸發器；鎖競爭會重新排程，來源讀取與 `MailApp` 失敗則保存錯誤並在約 30 分鐘後重試，達上限後控制臺明確顯示失敗。只有 `MailApp` 成功後才寫入已寄送；使用者先完成新學期設定時取消待寄信。舊版若因通知佇列而提前寫入已寄送，但佇列中仍有 `term_transition`，讀取時會清除誤標並改用專用觸發器。側欄預設恢復轉換前的自動同步偏好。
- 新課綱來源的當期新課程可出現在控制臺目錄，但不自動加入既有使用者選擇；24 小時未知標題觀察只保留於舊來源相容 helper，中央索引變更通知仍保留

## 狀態與安全模型

- 設定、來源觀察、通知狀態、受管理事件狀態與課綱最後成功快照儲存在 Script Properties；大型 JSON 會依 UTF-8 位元組切成每塊至多 7,500 bytes。分塊寫入以同一份 Properties 快照同時計算舊分塊數與容量，不再為舊計數增加一次服務呼叫。事件簽章只保存短雜湊、異動明細設上限、超過 120 天的歷史索引會移除，預估總量超過 430 KiB 時停止寫入。課綱快照以版本化 staging 寫入，完整驗證後才切換 active pointer，失敗不得覆蓋上一版。
- 每份 Google Docs 控制臺副本支援一個年級、一個通知信箱與一個由使用者自行擁有的同步目標 Calendar；主要日曆與其他自有日曆均可選擇，訂閱或非本人擁有的日曆仍拒絕。通用程式的 `DEFAULT_SETTINGS` 不得內含測試使用者 Email、行程選擇、Calendar ID 或預設年級；匯入前也不得讀課表、建立 Calendar、建立觸發器、同步、修復或刪除。
- 程式新建立的專用 Calendar 在建立當下設為顯示並套用 `#05a576`；列出、選取、保存或同步使用者既有 Calendar 時不得改變其主題色。
- Production 通用程式固定不含高負載測試函式與選單。開發者只有在執行 `scripts/generate-google-docs-control-panel.js --test-build` 時才會建立測試 artifact；測試版仍以單一「模擬控制臺首次同步」情境沿用正式分批、背景續跑與 watchdog 路徑。
- 控制臺不提供同步時段選項；`autoSyncHours` 固定保存四個窗口中心 `[3, 11, 18, 21]`，實際課表讀取時間由每份 Apps Script 副本固定分配在各中心前後一小時內。`instantNotificationsEnabled` 保存即時通知開關，`notificationHours` 保留使用者關閉即時通知後使用的自訂時間。讀取舊版設定時，會先把既有 `autoSyncHours` 遷移為 `notificationHours`，再套用固定同步窗口；沒有新欄位的安裝預設開啟即時通知。
- 控制臺同步選單提供「停止同步」與「移除受管理事件」。停止同步只關閉後續自動課表／課綱排程，保留既有事件與已開始的安全背景批次；刪除工具只處理已儲存的事件 ID，並驗證同步識別碼、管理標記與控制臺專屬權杖。「移除受管理事件」與「重設同步狀態」會持有同一把 Script Lock 完成背景工作檢查與變更，避免檢查後才開始同步的競態。
- `quickDeleteAllCalendarEvents()` 保持由 `ALLOW_QUICK_DELETE_ALL = false` 停用。
- 使用者輸入透過 `JSON.stringify` 與 U+2028／U+2029 escaping 安全序列化，不得將使用者字串直接串接進可執行程式碼。
- 通知收件者是一個純電子郵件地址；寄信失敗不得掩蓋原始同步失敗。
- 產生的 `Code.gs` 必須保留 `@OnlyCurrentDoc`；`appsscript.json` 的 OAuth scope 必須精確維持目前功能所需的 8 項：`documents.currentonly`、`spreadsheets.readonly`、Calendar 管理、寄信、外部讀取、Trigger、容器 UI 與目前帳號 Email，並只啟用 Sheets v4 進階服務。不得加入完整 Docs／Sheets、Drive、Gmail 或其他未審核權限。
- 課表來源只在單次 Apps Script 執行內依年級快取成功的解析結果；同一個「儲存並同步」流程重複取用相同快照，下一次手動或背景執行仍必須重新下載，不能以跨執行快取犧牲資料新鮮度。
- Calendar 選單每個項目只讀取一次 ID；課綱索引與課綱分頁透過 Sheets API 批次讀取實際使用範圍，保留原空表檢查，並在每張分頁只整理一次日期候選，減少 Google 服務呼叫及重複列運算而不改變資料範圍。
- 安裝器 CSP 應維持最小權限，只在必要時增加明確來源。

## 部署與快取

根目錄 `index.html` 以主要樣式表網址中的固定發布版本建立 `TSCHOOL_ASSET_VERSION`，並將同一查詢值加到下列檔案：

- `styles.css`
- `schedule-data.js`
- `setup-code.js`
- `app.js`

上述公開資產共用同一固定快取版本。網站不載入 `sidebar-template.js`、`setup-dialog-template.js` 或 `code-template.js`；這些只供開發者產生 Google Docs 母版程式。`app.js` 等核心依賴完成後即可啟動，最後一步只需使用 `setup-code.js` 產生設定碼。

`styles.css` 必須在 `<head>` 直接提供 `href`，讓瀏覽器在首次繪製前完成主要樣式載入，避免顯示裸露 HTML。不得使用 `Date.now()` 為每次瀏覽產生新版本；發布前若上述資產有變更，應更新 `styles.css?v=...` 的固定版本值，讓瀏覽器能沿用同一版快取，又能在新版部署後正確失效。

## 長期限制

- 公開設定產生器維持靜態、免建置且無外部執行階段依賴。
- Calendar 配額安全與可復原性優先於最大寫入速度。
- 不得還原與任務無關的未提交使用者修改。
- 所有使用者均使用 Google Docs 控制臺；課表 Sheet 與 Sheet 控制臺已停止使用，不再規劃其相容或遷移分支；課程大綱 Sheets 維持既有獨立資料來源
- 正規化來源契約位於 `source-contract.js`，私人發布專案保存相同版本的契約；正式 Cloudflare origin 尚未設定，`NORMALIZED_API_ORIGIN` 保持空字串，必須完成真實來源探針與 staging 驗收後才切換
- 正規化來源在讀取時檢查同步許可與36小時有效期限，每個 Calendar 批次及每筆事件新增、修改與自動刪除前重新取得目前許可；暫停不授權清除現有事件與設定

## 2026-09-18 本機擷取停止決策

- 使用者認為專用 Chrome、定期背景工作及本機課表擷取干擾正常使用電腦，已要求取消整套在本機執行的擷取流程
- 2026-09-18 已移除 `io.tschool.schedule-source` LaunchAgent，確認登入服務中沒有此工作、本機擷取與審核程序為零、審核頁不再監聽；私人 repo 的程式碼與 repo 外歷史狀態保留作紀錄
- 相鄰私人專案 `../t-school-schedule-source` 保留已完成的程式與測試作為歷史成果；不得因其存在就重新安裝 LaunchAgent、啟動 Playwright 擷取、執行 shadow／正式 collect，或要求使用者以個人 Mac 維持定期工作
- Cloudflare staging 曾部署並完成首次人工核准，但這不代表正式來源已切換；公開專案的 `NORMALIZED_API_ORIGIN` 仍是空字串，Google Docs／Calendar 的新來源真實驗收尚未完成
- 後續若要繼續來源更新，先與使用者討論不依賴其個人 Mac 持續執行的替代方案；既有課表來源安全檢查及課程大綱 Sheets 的獨立讀取邊界仍須維持
